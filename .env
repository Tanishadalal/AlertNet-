PORT=3000
JWT_SECRET=emergency_alert_jwt_secret_key_2024

# Optional: Configure SMTP for real email verification
# If not set, verification link will be shown in terminal (dev mode)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your@gmail.com
# SMTP_PASS=your_app_password

# For testing with Ethereal (auto fake SMTP):
# Leave SMTP fields blank to use dev mode (link printed to console)
